---
config:
  layout: dagre
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
---
sequenceDiagram
    autonumber
    participant Anchor as 主播 (View)
    participant Ctrl as 控制层 (Controller)
    participant Srv as 服务层 (Service)
    participant DB as 持久层 (MySQL)

%%     Note over Anchor, DB: 直播创建与状态激活流程
    Anchor->>Ctrl: 1: 提交直播信息 (标题、描述)
    Ctrl->>Ctrl: 2: 验证主播 JWT 身份令牌
    Ctrl->>Srv: 3: 请求创建实时活动任务
    Srv->>Srv: 4: 生成唯一直播 ID 与流密钥
    Srv->>DB: 5: 写入直播间基础档案数据
    DB-->>Srv: 6: 反馈存储成功响应
    Srv-->>Ctrl: 7: 确认直播任务激活成功
    Ctrl->>Ctrl: 8: 触发实时活动情况更改通知
    Ctrl-->>Anchor: 9: 返回推流地址并跳转推流页