---
config:
  layout: dagre
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
---
sequenceDiagram
    autonumber
    participant User as 观众 (View)
    participant Ctrl as 控制层 (Controller)
    participant Srv as 服务层 (Service)
    participant DB as 持久层 (MySQL)

%%     Note over User, DB: 直播间列表请求与动态渲染流程
    User->>Ctrl: 1: 访问首页并请求直播列表
    Ctrl->>Srv: 2: 调用直播列表查询接口
    Srv->>DB: 3: 检索状态为 "is_live" 的活动记录
    DB-->>Srv: 4: 返回符合条件的直播档案数据
    Srv->>Srv: 5: 封装数据为 LiveEntity 集合
    Srv-->>Ctrl: 6: 反馈处理完毕的列表数据
    Ctrl-->>User: 7: 下发 JSON 数据并渲染列表卡片
    
    loop 实时状态监听
        Ctrl->>Srv: 8: 监测实时活动情况更改
        Srv-->>User: 9: 推送状态更新通知 (WebSocket)
        User->>User: 10: 动态刷新首页展示活动列表
    end