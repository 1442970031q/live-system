---
config:
  layout: elk
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
    fontSize: 18px
---
graph TD
    %% 视图层
    subgraph View ["View层 (前端表现层)"]
        direction TB
        subgraph Client ["客户端应用 (client/src)"]
            direction TB
            Client_UI["前端页面与交互组件 (React)"]
            Player["流媒体播放器组件 (flv.js)"]
            WS_Client["弹幕通信组件 (WebSocket Client)"]
        end
        Admin["管理端后台应用 (React)<br>admin/src"]
    end

    %% 控制层
    subgraph Controller ["Controller层 (接口与路由控制)"]
        direction TB
        Api_Routes["Express 路由 (HTTP API)<br>routes/auth.js, live.js..."]
        Sens_Ctrl["风控控制器<br>sensitiveController.js"]
        WSS["弹幕推送网关<br>webSocketServer.js"]
        NMS["流媒体网关<br>mediaServer.js (RTMP/HTTP-FLV)"]
    end

    %% 服务层
    subgraph Service ["Service层 (核心业务逻辑层)"]
        direction TB
        Biz_Service["通用业务逻辑<br>(用户管理/直播间管理)"]
        DFA_Pool["多线程敏感词过滤系统<br>WorkerPool + DFAFilter"]
        Sens_Service["风控调度服务<br>sensitiveService.js"]
        ASR_Service["语音转文本微服务<br>(Python Flask/FastAPI app.py)"]
    end

    %% 数据访问层
    subgraph DAO ["DAO层 (数据访问层)"]
        direction TB
        DB_Core["MySQL 连接池与封装<br>server/db.js"]
    end

    %% 持久化层
    subgraph Storage ["持久化数据存储层"]
        direction TB
        MySQL[("MySQL 关系型数据库<br>(用户信息、直播记录、敏感词库)")]
        Local_Media[("文件系统<br>(截取的主播语音片段 temp.wav)")]
    end

    %% 数据流转连线
    Client_UI -- "HTTP / REST" --> Api_Routes
    Admin -- "HTTP / REST" --> Sens_Ctrl
    Admin -- "HTTP / REST" --> Api_Routes
    WS_Client -. "WebSocket" .-> WSS
    Player -. "拉流 (FLV)" .-> NMS

    Api_Routes --> Biz_Service
    Sens_Ctrl --> Sens_Service
    WSS -- "提取实时弹幕" --> Sens_Service
    NMS -- "提取音频流切片" --> ASR_Service
    
    ASR_Service -- "语音转文本结果" --> Sens_Service
    Sens_Service -- "分发 Worker 线程" --> DFA_Pool

    Biz_Service --> DB_Core
    Sens_Service --> DB_Core
    DFA_Pool -. "同步黑白名单" .- DB_Core

    DB_Core --> MySQL
    ASR_Service --> Local_Media
    
    classDef layerFill fill:#f9f9f9,stroke:#333,stroke-width:2px,stroke-dasharray: 5 5;
    class View,Controller,Service,DAO,Storage layerFill;