---
config:
  layout: elk
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
    fontSize: 18px
---
classDiagram
    class DanmakuComponent {
        +state messages: Array
        +state comments: Array
        +state rowIndex: number
        +method fetchComments(streamId): void
        +method addDanmaku(id, text, color, speed): void
        +method getRowPosition(): number
        -FILTER_IDS: Array
        -totalRows: number
    }

    class GetALLCommentAPI {
        ...
        +method getComments(streamId): Promise
        +method onMessage(callback): void
    }

    class CommentAPI {
         ...
        +method sendComment(streamId, content): Promise
        +method getComments(streamId): Promise
    }

    class CommentController {
        ...
        +method postComment(streamId, userId, content): Promise
        +method getCommentsByStreamId(streamId): Promise
    }

    class CommentService {
        ...
        +method createComment(streamId, userId, content): Promise
        +method getStreamComments(streamId): Promise
        +method validateContent(content): boolean
    }

    class Comment {
        +id: number
        +streamId: number
        +userId: number
        +content: string
        +createdAt: Date
        +username: string
    }

    class CommentDatabase {
        ...
        +method insert(commentData): Promise
        +method queryByStreamId(streamId): Promise
        +table comments
    }

    DanmakuComponent --> GetALLCommentAPI : uses
    DanmakuComponent --> CommentAPI : uses
    GetALLCommentAPI --> CommentController : communicates with
    CommentAPI --> CommentController : calls
    CommentController --> CommentService : delegates
    CommentService --> CommentDatabase : operates on
    CommentService --> Comment : creates/returns
    CommentDatabase --> Comment : stores