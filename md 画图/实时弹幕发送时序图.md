---
config:
  layout: dagre
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
---
sequenceDiagram
    autonumber
    participant Audience as 观众 (View)
    participant Ctrl as 控制层 (Controller)
    participant Srv as 服务层 (Service)
    participant DB as 持久层 (MySQL)

    %% Note over Audience, DB: 实时弹幕发送与内容审查流程
    Audience->>Ctrl: 1: 提交弹幕评价信息 (携带 JWT)
    Ctrl->>Ctrl: 2: 验证发送者身份权限
    Ctrl->>Srv: 3: 调用内容筛选审查接口
    Srv->>Srv: 4: 执行敏感词过滤与合规性检查

    
    alt 审查通过
        Srv->>DB: 5: 持久化存储弹幕记录 (ChatEntity)
        DB-->>Srv: 6: 反馈存储成功响应
        Srv-->>Ctrl: 7: 确认弹幕发布成功
        Ctrl-->>Audience: 8: 返回成功状态码
        Note right of Ctrl: 触发实时互动情况更新
    else 审查不通过
        Srv-->>Ctrl: 9: 反馈违规内容拦截信息
        Ctrl-->>Audience: 10: 提示评价内容不合法
    end