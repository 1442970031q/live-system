---
config:
  layout: dagre
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
---
graph TD
    A[用户输入账号密码] --> B{前端格式检查}
    B -- 失败 --> A
    B -- 成功 --> C[发送登录请求]
    C --> D[后端调取加密凭证]
    D --> E{密码比对校验}
    E -- 错误 --> F[返回错误提示]
    F --> A
    E -- 正确 --> G[签发数字令牌]
    G --> H[令牌存入浏览器本地]
    H --> I[完成身份认证]