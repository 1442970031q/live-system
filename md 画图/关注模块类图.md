---
config:
  layout: elk
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
    fontSize: 18px
---
classDiagram
    class User {
        +id: number
        +username: string
        +avatar: string
        +getFollows(): Follow[]
        +getFollowers(): Follow[]
    }

    class Follow {
        +id: number
        +userId: number
        +followId: number
        +createdAt: Date
        +isFollowing(): boolean
    }

    class FollowService {
        ...
        +followUser(userId: number, followId: number): Promise<Follow>
        +unfollowUser(userId: number, followId: number): Promise<void>
        +getUserFollows(userId: number): Promise<User[]>
        +checkIfFollowing(userId: number, followId: number): Promise<boolean>
    }

    class UserAPI {
        ...
        +followUser(followId: number): Promise<void>
        +unfollowUser(followId: number): Promise<void>
        +getFollows(): Promise<User[]>
        +checkFollowStatus(followId: number): Promise<boolean>
    }

    class FollowButton {
        +streamUserId: number
        +isFollowing: boolean
        +isLoading: boolean
        +handleFollowToggle(): void
    }

    class FollowsList {
        +userId: number
        +follows: User[]
        +fetchFollows(): void
        +renderFollowItems(): JSX.Element[]
    }

    User "1" -- "*" Follow : has
    FollowService --> Follow : manages
    UserAPI --> FollowService : calls
    FollowButton --> UserAPI : uses
    FollowsList --> UserAPI : uses
    Follow "1" -- "1" User : targets >