---
config:
  layout: dagre
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
---
graph TD
    A[观众输入弹幕内容] --> B{校验通行令牌}
    B -- 未登录 --> C[显示登录提示]
    B -- 已登录 --> D[关联用户与弹幕信息]
    D --> E[数据存入弹幕数据库]
    E --> F[前端定时监控任务启动]
    F --> G[拉取服务器最新记录]
    G --> H[计算弹幕飞行轨道]
    H --> I[视频上方滚动显示]