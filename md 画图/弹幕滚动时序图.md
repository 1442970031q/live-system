---
config:
  layout: dagre
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
---
sequenceDiagram
    autonumber
    participant Audience as 观众 (View)
    participant Ctrl as 控制层 (DanmakuController)
    participant Srv as 服务层 (DanmakuServiceImpl)
    participant DB as 持久层 (MySQL)

    %% Note over Audience, DB: 弹幕滚动展示与动态渲染流程
    Audience->>Ctrl: 1: 进入直播间并订阅弹幕消息流
    Ctrl->>Srv: 2: 请求当前展示活动的活跃评价数据
    Srv->>DB: 3: 检索该 stream_id 下的最新评论记录
    DB-->>Srv: 4: 返回评价数据集合 (ChatEntity)
    Srv->>Srv: 5: 计算弹幕轨道、颜色与动画速度
    Srv-->>Ctrl: 6: 封装并反馈展示数据 (DanmakuEntity)
    Ctrl-->>Audience: 7: 通过 WebSocket/Polling 分发展示数据
    
    loop 动态滚动过程
        Audience->>Audience: 8: 根据 DanmakuEntity 挂载 React 组件
        Audience->>Audience: 9: 触发 CSS Translate 动画进行滚动展示
        Audience->>Audience: 10: 动画结束后自动卸载弹幕 DOM 节点
    end