---
config:
  layout: elk
  theme: redux
  look: classic
  themeVariables:
    edgeLabelBackground: '#fff'
    fontSize: 18px
---
classDiagram
    class Stream {
        +id: number
        +userId: number
        +title: string
        ...
        +create(): Stream
        +end(): void
        +getDetails(): Stream
        +getList(): Stream[]
    }

    class User {
        +id: number
        ...
        +createStream(streamData): Stream
        +getLiveStreams(): Stream[]
    }

    class Comment {
        +id: number
        +streamId: number
        +userId: number
        ...
        +send(): Comment
        +getByStreamId(streamId): Comment[]
    }

    class StreamView {
        +id: number
        +streamId: number
        ...
        +recordView(): void
    }

    class CreateStreamComponent {
        +formData: title,desc
        +streamId: number
        +isConnected: boolean
        +handleChange(): void
        +startStreaming(wsUrl): void
        +handleStartStream(): void
    }

    class LiveAPI {
        +createStream(formData): Promise<streamId, wsUrl>
        +endStream(streamId): Promise<void>
        +getStreamList(): Promise<Stream[]>
        +getStreamDetails(streamId): Promise<Stream>
    }

    class WebSocketService {
        +socket: WebSocket
        +createConnection(wsUrl): WebSocket
        +sendData(data): void
        +onMessage(callback): void
        +closeConnection(): void
    }

    User "1" -- "*" Stream : owns
    Stream "1" -- "*" Comment : has
    Stream "1" -- "*" StreamView : records
    CreateStreamComponent -- LiveAPI : uses
    CreateStreamComponent -- WebSocketService : uses
    WebSocketService -- Stream : pushes data to
    LiveAPI -- Stream : manipulates
    Comment -- Stream : belongs to